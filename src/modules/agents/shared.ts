/**
 * Shared types between agents — re-exports so adapters and consumers
 * pull from a single import path.
 */
export type { SessionMeta } from "../transcript/discover.js";
export type { ParsedSession } from "../transcript/parse.js";
