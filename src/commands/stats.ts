/**
 * `sipcode stats` — cumulative savings across sessions.
 * Implementation deferred to v0.1.0 stabilization phase.
 */
export async function runStats(): Promise<void> {
  console.log("sipcode stats — not yet implemented (planned for v0.1.0 stabilization).");
}
