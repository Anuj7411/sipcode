/**
 * `sipcode init` — interactive project setup.
 * Implementation deferred to v0.1.0-alpha.2 (after `why` ships).
 *
 * See docs/SESSION-HANDOFF.md §8 and docs/ROADMAP.md for sequencing.
 */
export async function runInit(): Promise<void> {
  console.log("sipcode init — not yet implemented (planned for v0.1.0-alpha.2).");
}
