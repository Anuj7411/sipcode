# average

This is a moderately well-organized project used as the middle fixture for
sipcode score testing. It intentionally fails a handful of checks so the
score lands somewhere in the silver band.

Run `node src/main.js` to start the app.
